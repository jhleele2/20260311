# Supabase + Vercel 설정 한 번에 따라하기

로또 추첨 결과를 Supabase에 저장하고, Vercel에 배포해서 환경 변수로 쓰기까지 순서대로 정리했습니다.

---

## 전체 순서 (요약)

1. **Supabase** — 프로젝트 만들기 → 테이블 생성 → URL·키 복사  
2. **Vercel** — 프로젝트 배포 → 환경 변수 추가 → 재배포  
3. **확인** — 배포된 사이트에서 추첨 후 Supabase에 데이터 들어오는지 확인  

---

## 1. Supabase 세팅

### 1-1. 프로젝트 만들기

1. [supabase.com](https://supabase.com) 접속 후 로그인  
2. **New Project** 클릭  
3. **Organization** 선택(없으면 생성), **Name** 입력(예: `my-lotto`)  
4. **Database Password** 설정 후 기억해 두기(나중에 DB 접속 시 필요)  
5. **Create new project** 클릭 → 생성 완료될 때까지 대기  

### 1-2. 테이블 만들기 (SQL 한 번에)

1. 왼쪽 메뉴에서 **SQL Editor** 클릭  
2. **New query** 선택  
3. 아래 SQL 전체 복사해서 붙여넣기 후 **Run** (또는 Ctrl+Enter)  

```sql
create table public.lotto_draws (
  id bigint generated by default as identity primary key,
  numbers smallint[] not null,
  bonus smallint not null,
  created_at timestamptz default now()
);

alter table public.lotto_draws enable row level security;

create policy "Allow anonymous insert"
  on public.lotto_draws for insert
  to anon
  with check (true);

create policy "Allow read"
  on public.lotto_draws for select
  to anon
  using (true);
```

4. 성공하면 **Table Editor** → `lotto_draws` 테이블이 보입니다.  

### 1-3. URL이랑 키 복사해 두기

1. 왼쪽 아래 **Project Settings**(톱니바퀴) 클릭  
2. **API** 메뉴 선택  
3. 아래 두 개 복사해서 메모장 등에 잠깐 붙여넣어 두기  
   - **Project URL**  
     - 예: `https://abcdefghijk.supabase.co`  
   - **Project API keys** 중 **anon** **public**  
     - 긴 JWT 문자열 (eyJ로 시작)  

나중에 Vercel 환경 변수에 그대로 넣을 거예요.  

---

## 2. Vercel 세팅

### 2-1. 프로젝트 배포하기 (Git 사용)

1. [vercel.com](https://vercel.com) 로그인  
2. **Add New** → **Project**  
3. **Import Git Repository**에서 GitHub 저장소 선택  
   - 이 프로젝트가 있는 저장소(예: `jhleele2/20260311`) 선택  
4. **Root Directory**는 비워 두거나 `./`  
5. **Deploy** 클릭 → 배포 끝날 때까지 대기  
6. 배포되면 **Visit** 또는 주소(예: `https://20260311-pink.vercel.app`) 로 접속 가능  

### 2-2. 환경 변수 넣기 (Supabase URL·키)

1. Vercel 대시보드에서 **해당 프로젝트** 클릭  
2. 위쪽 메뉴에서 **Settings** 클릭  
3. 왼쪽에서 **Environment Variables** 클릭  
4. 아래처럼 **두 개** 추가  

**첫 번째**

- **Key**: `SUPABASE_URL`  
- **Value**: 아까 복사한 Supabase **Project URL** 전체  
  - 예: `https://abcdefghijk.supabase.co`  
- **Environment**: Production, Preview, Development 전부 체크  

**Add** 클릭  

**두 번째**

- **Key**: `SUPABASE_ANON_KEY`  
- **Value**: 아까 복사한 Supabase **anon public** 키 전체  
- **Environment**: Production, Preview, Development 전부 체크  

**Add** 클릭  

5. **Save** 되어 있는지 확인  

### 2-3. 재배포해서 환경 변수 적용

환경 변수는 **새로 배포**해야 적용됩니다.  

1. 위쪽 메뉴 **Deployments** 클릭  
2. 맨 위(가장 최근) 배포 오른쪽 **⋯** (점 세 개) 클릭  
3. **Redeploy** 선택  
4. **Redeploy** 한 번 더 클릭  
5. 배포가 끝나면 해당 URL로 다시 접속  

---

## 3. 확인

1. **배포된 사이트**(예: `https://20260311-pink.vercel.app`) 접속  
2. **로또 추첨** 버튼 눌러서 한 번 추첨  
3. **Supabase** → **Table Editor** → `lotto_draws`  
   - 방금 추첨한 번호·보너스·시각이 한 행으로 들어오면 성공  

---

## 문제 해결

- **추첨해도 Supabase에 안 쌓여요**  
  - Vercel **Environment Variables**에 `SUPABASE_URL`, `SUPABASE_ANON_KEY` 넣었는지 확인  
  - 넣었다면 **Redeploy** 한 번 했는지 확인  
  - 브라우저 개발자 도구(F12) → **Console**에 에러 있는지 확인  

- **로컬에서 index.html만 열었을 때**  
  - `/api/config`가 없어서 Supabase 저장은 안 됩니다.  
  - 로컬에서도 저장 쓰려면 터미널에서 `vercel dev` 실행 후 나오는 주소로 접속하세요.  

- **Supabase 테이블/RLS를 다시 만들고 싶을 때**  
  - `SUPABASE_로또저장_가이드.md` 의 **2. 테이블 SQL로 한 번에 만들기** 참고.  

---

정리하면: **Supabase에서 프로젝트 + 테이블 + URL·키** → **Vercel에서 배포 + 환경 변수 2개 + Redeploy** 하면 끝입니다.
