# Supabase에 로또 번호 저장하기

## HTML에 반영된 내용

- `index.html`에 Supabase 스크립트와 저장 로직이 들어가 있습니다.
- 추첨이 끝나면 **자동으로** `lotto_draws` 테이블에 저장됩니다.
- 아래 1~2단계만 완료하고, 3단계에서 **URL·키만 넣으면** 바로 동작합니다.

---

## Supabase에 저장하는 방법 (순서대로)

### 1. Supabase 프로젝트 설정

1. [Supabase](https://supabase.com) 로그인 후 **New Project** 로 프로젝트 생성
2. **Table Editor** → **New Table**
   - Name: `lotto_draws` (또는 원하는 이름)
   - 아래 컬럼 추가 후 **Save**

| Column name | Type | Default | 설명 |
|-------------|------|---------|------|
| id | int8 | auto (Identity) | 자동 증가 PK |
| numbers | int8[] | - | 본 번호 6개 배열 |
| bonus | int2 | - | 보너스 번호 |
| created_at | timestamptz | now() | 추첨 시각 |

3. **Authentication** → **Policies** (또는 Table에서 RLS 설정)
   - `lotto_draws` 테이블에 **Insert** 허용 정책 추가 (anon으로 insert 허용하면 비로그인도 저장 가능)
   - 읽기만 필요하면 **Select** 정책만 추가

4. **Project Settings** → **API** 에서 확인
   - **Project URL** (예: `https://xxxx.supabase.co`)
   - **anon public** 키 복사

---

### 2. 테이블 SQL로 한 번에 만들기

**SQL Editor**에서 아래 실행하면 테이블이 생성됩니다.

```sql
create table public.lotto_draws (
  id bigint generated by default as identity primary key,
  numbers smallint[] not null,
  bonus smallint not null,
  created_at timestamptz default now()
);

-- 비로그인 사용자도 insert 가능하게 (선택)
alter table public.lotto_draws enable row level security;

create policy "Allow anonymous insert"
  on public.lotto_draws for insert
  to anon
  with check (true);

create policy "Allow read"
  on public.lotto_draws for select
  to anon
  using (true);
```

---

### 3. index.html에 URL·키 넣기

1. Supabase 대시보드 → **Project Settings** (왼쪽 아래 톱니바퀴) → **API**
2. **Project URL** 복사 → `index.html` 열어서 `SUPABASE_URL` 값에 붙여넣기 (따옴표 안에)
3. **anon public** 키 복사 → `SUPABASE_ANON_KEY` 값에 붙여넣기

**Q: 웹페이지 URL(Site URL) 설정이 필요할까요?**  
**A: 이 프로젝트는 로그인 없이 번호만 저장하므로 웹페이지 URL 설정은 하지 않아도 됩니다.**  
- `index.html`을 그냥 더블클릭해서 열거나, 로컬 서버로 열어도 anon key만 있으면 저장됩니다.  
- 나중에 로그인( Supabase Auth )을 쓸 때만 **Authentication → URL Configuration** 에서 Site URL / Redirect URL 을 넣으면 됩니다.

```javascript
// 예시 (본인 값으로 교체)
const SUPABASE_URL = 'https://abcdefgh.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6...';
```

저장 후 페이지 새로고침하고 추첨하면 Supabase **Table Editor** → `lotto_draws` 에 행이 쌓입니다.

#### 3-1. Vercel 배포 시 (Environment Variable)

배포된 사이트에서는 코드에 URL·키를 넣지 않고 **Vercel 환경 변수**로 설정합니다.

1. **Vercel** 대시보드 → 해당 프로젝트 → **Settings** → **Environment Variables**
2. 다음 두 개 추가:

| Name | Value | Environment |
|------|--------|--------------|
| `SUPABASE_URL` | `https://xxxx.supabase.co` (Supabase Project URL) | Production, Preview, Development |
| `SUPABASE_ANON_KEY` | Supabase anon public 키 | Production, Preview, Development |

3. **Save** 후 **Redeploy** (Deployments → … → Redeploy) 한 번 하면 적용됩니다.

동작: `api/config.js`가 요청 시 위 환경 변수를 읽어 JSON으로 내려주고, HTML에서는 `/api/config`를 호출해 Supabase 클라이언트를 만듭니다.  
**로컬에서만 테스트할 때**: `index.html`을 파일로 열면 `/api/config`가 없어 저장이 되지 않습니다. 로컬에서도 저장을 쓰려면 `vercel dev`로 실행하거나, 위처럼 HTML에 URL·키를 잠시 넣어 테스트할 수 있습니다.

---

## (참고) 웹에서 저장하는 코드 구조

배포 환경에서는 `index.html`이 `/api/config`를 fetch한 뒤 그 값으로 Supabase 클라이언트를 생성합니다.

```javascript
// Vercel 배포 시: api/config에서 SUPABASE_URL, SUPABASE_ANON_KEY 전달
let supabase = null;
(async function initSupabase() {
  const { SUPABASE_URL, SUPABASE_ANON_KEY } = await fetch('/api/config').then(r => r.json());
  if (SUPABASE_URL && SUPABASE_ANON_KEY) supabase = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
})();
// 로컬 테스트 시: 아래처럼 직접 넣어도 됨
// const SUPABASE_URL = 'https://여기에프로젝트ID.supabase.co';
// const SUPABASE_ANON_KEY = '여기에_anon_키_붙여넣기';
// const supabase = window.supabase?.createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

async function saveLottoToSupabase(numbers, bonus) {
  if (!supabase) return;
  const { data, error } = await supabase
    .from('lotto_draws')
    .insert([{ numbers, bonus }])
    .select();
  if (error) console.error('Supabase 저장 실패:', error);
  return data;
}
```

---

## 4. 저장된 데이터 확인

- Supabase **Table Editor**에서 `lotto_draws` 테이블 선택
- 또는 **SQL Editor**에서:

```sql
select * from public.lotto_draws order by created_at desc limit 20;
```

---

## 5. 보안 참고

- **anon key**는 프론트에 노출되는 공개 키라서, RLS로 insert/select만 허용하고 수정·삭제는 막는 것이 좋습니다.
- 로그인 사용자만 저장하게 하려면 `to anon` 대신 `to authenticated` 로 정책을 바꾸고, Supabase Auth 로그인 후 insert 하면 됩니다.
