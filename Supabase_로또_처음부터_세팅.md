# Supabase에 로또 번호 저장하기 — 처음부터 세팅

로또 추첨 결과를 Supabase에 저장하려면 아래 순서대로만 하면 됩니다.

---

## 1단계: Supabase 프로젝트 만들기

1. **https://supabase.com** 접속 후 로그인(또는 회원가입).
2. **New project** 클릭.
3. **Organization** 선택(없으면 Create new).
4. **Name**에 프로젝트 이름 입력(예: `lotto-save`).
5. **Database Password** 입력 후 반드시 저장해 두기.
6. **Create new project** 클릭 → 프로젝트가 준비될 때까지 1~2분 대기.

---

## 2단계: 로또 저장용 테이블 만들기

1. 왼쪽 메뉴에서 **SQL Editor** 클릭.
2. **New query** 선택.
3. 아래 SQL **전체** 복사해서 붙여넣기.

```sql
-- 로또 추첨 결과 저장 테이블
create table public.lotto_draws (
  id          bigint generated by default as identity primary key,
  numbers     smallint[] not null,   -- 본 번호 6개 (배열)
  bonus       smallint not null,      -- 보너스 번호
  created_at  timestamptz default now()
);

-- 보안: RLS 켜기
alter table public.lotto_draws enable row level security;

-- 비로그인(anon)도 insert 허용 → 웹에서 추첨하면 저장 가능
create policy "Allow anonymous insert"
  on public.lotto_draws for insert
  to anon
  with check (true);

-- 조회도 허용 (필요하면)
create policy "Allow read"
  on public.lotto_draws for select
  to anon
  using (true);
```

4. **Run** (또는 Ctrl+Enter) 실행.
5. 성공하면 왼쪽 **Table Editor**에 `lotto_draws` 테이블이 생깁니다.

---

## 3단계: API URL이랑 키 복사하기

웹페이지에서 Supabase에 접속하려면 **Project URL**과 **anon 키**가 필요합니다.

1. 왼쪽 아래 **Project Settings**(톱니바퀴) 클릭.
2. **API** 메뉴 선택.
3. 아래 두 값을 복사해서 메모장 등에 붙여넣어 두기.

| 항목 | 위치 | 용도 |
|------|------|------|
| **Project URL** | Configuration → Project URL | 웹에서 Supabase 주소 |
| **anon public** 키 | Project API keys → anon public | 웹에서 DB 접근용 (공개 키) |

- **Project URL** 예: `https://abcdefghijk.supabase.co`
- **anon public** 예: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...` (긴 문자열)

---

## 4단계: 웹/앱에 연결하기

### 로컬에서 테스트할 때 (HTML 파일만 쓸 때)

`index.html`처럼 Supabase를 쓰는 코드가 있다면, 아래 두 값을 **본인 프로젝트 값으로** 바꿉니다.

- `SUPABASE_URL` → 위에서 복사한 **Project URL**
- `SUPABASE_ANON_KEY` → 위에서 복사한 **anon public** 키

이 프로젝트는 이미 **Vercel 환경 변수**로 받도록 되어 있으므로, **배포한 사이트**에서는 HTML에 직접 넣지 않고, Vercel에만 넣으면 됩니다.

### Vercel에 배포했을 때

- Vercel **Settings** → **Environment Variables**에 다음 두 개만 추가하면 됩니다.
  - **Key**: `SUPABASE_URL` → **Value**: Project URL
  - **Key**: `SUPABASE_ANON_KEY` → **Value**: anon public 키
- 저장 후 **Deployments**에서 **Redeploy** 한 번 하면 적용됩니다.

---

## 5단계: 저장 확인

1. 웹에서 **로또 추첨** 한 번 실행.
2. Supabase 대시보드 → **Table Editor** → **lotto_draws** 선택.
3. 방금 추첨한 **numbers**, **bonus**, **created_at** 행이 보이면 세팅 완료.

---

## 정리

| 순서 | 할 일 |
|------|--------|
| 1 | Supabase에서 New project로 프로젝트 생성 |
| 2 | SQL Editor에서 `lotto_draws` 테이블 + RLS 정책 실행 |
| 3 | Project Settings → API에서 Project URL, anon public 키 복사 |
| 4 | 로컬: HTML에 URL·키 넣기 / 배포: Vercel 환경 변수에 넣고 Redeploy |
| 5 | 추첨 후 Table Editor에서 데이터 확인 |

**추가로 할 일 없음**  
- Authentication(Site URL 등) 설정 안 해도 됨.  
- 로그인 없이 번호만 저장하는 구조라 anon 키만 있으면 됩니다.
